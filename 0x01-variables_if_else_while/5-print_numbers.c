#include <stdio.h>

/**
 * main - display numbers from 0 to 9
 * Return: return 0 and exit the program.
*/

int main(void)
{
	int x;

	for (x = 0; x < 10; x++)
	{
		printf("%d", x);
	}
	printf("\n");

	return (0);
}
